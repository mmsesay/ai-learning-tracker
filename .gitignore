# Long-form writing lives in the private Maej Library (mmsesay/maej-library).
# Keep ignoring accidental local article folders so they never enter this public repo.
articles/
ai-learning/

# Local Cursor MCP config (machine-specific paths)
LOCAL-MCP-JSON.md
